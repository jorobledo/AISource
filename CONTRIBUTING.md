# Contributing

This repository is still defining its benchmark protocol.

Before adding code, open an issue describing the dataset, model, metric, or
workflow decision. Keep changes small and update the relevant template.

Do not commit raw or restricted MCPL files. Run these checks before submitting:

```bash
ruff check .
pytest
```

The project license must be selected before the first public software release.
